Endpunkt:
  allOf:
    - $ref: './Ablaufpunkt.yaml#/Ablaufpunkt'
    - type: object
      xml:
        name: 'Endpunkt'
      required:
        - version
      properties:
        version:
          type: string
          xml:
            attribute: true