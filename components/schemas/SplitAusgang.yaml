SplitAusgang:
  allOf:
    - $ref: './Ablaufpunkt.yaml#/Ablaufpunkt'
    - type: object
      xml:
        name: 'SplitAusgang'
      required:
        - version
        - name
      properties:
        version:
          type: string
          xml:
            attribute: true
        name:
          type: string
          xml:
            attribute: true